node_modules/
.next/
.env

# Fully generated by scripts/export-static.mjs at build time (Markdown mirror + llms.txt).
public/

# translate-messages tool cache (dossier + pending-batch state).
scripts/translate-messages/.cache/
