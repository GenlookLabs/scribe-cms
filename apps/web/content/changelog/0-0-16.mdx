---
version: "0.0.16"
publishedAt: "2026-07-06"
---

## Fixed

- The target locale is now always appended to translation prompts, even when a custom `translate.prompt` is configured, so the model can no longer lose track of which language it is translating into
